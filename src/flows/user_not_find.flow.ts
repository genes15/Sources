import { addKeyword,EVENTS } from "@builderbot/bot";

export default addKeyword(EVENTS.ACTION)
.addAnswer('🚫 No me apareces en la base de datos, Por favor dirígete a la sede de Gestión Humana de tu ciudad')