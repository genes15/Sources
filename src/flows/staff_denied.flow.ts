import { addKeyword,EVENTS } from "@builderbot/bot";


export default addKeyword(EVENTS.ACTION)
.addAnswer('🚷 Usted esta intentando expedir la carta laborar de una persona retirada, este proceso de debe realizar desde la cede de gestion humana mas cercana')
